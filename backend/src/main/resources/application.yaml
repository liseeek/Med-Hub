server:
  port: 8080
spring:
  datasource:
    driverClassName: org.postgresql.Driver
  sql:
    init:
      platform: postgres
      mode: never
  jpa:
    database: postgresql
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true